services:
  - type: web
    name: collegechatbot
    runtime: python
    rootDir: college_chatbot
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9